---
title: Dollar
description: equations in babeldown
---

an online equation : $a = b + c$ is ok; or two for that matter $e_t = e_{t-1} + \varepsilon_t$

but $a$ a one-character equation, no!



[^11] it's corrected now footnotes and an equation$e_t = e_{t-1} + \varepsilon_t$

a block equation :

$$
a = b + c
$$

```{r, echo=FALSE, message=FALSE, warning=FALSE}
#| label: fig-reference
#| fig-cap: Une figure
library(tidyverse)
ggplot(cars)+geom_point(aes(x=speed,y=dist))
```


